version: '3'

services:
  app:
    build:
      context: '.'
      dockerfile: Dockerfile
    environment:
      - ISO=0
      - PACKAGE=${PACKAGE}
      - REDIS_HOST=localhost
    ports:
      - "5000:5000"
